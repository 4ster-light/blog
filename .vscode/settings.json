{
  "deno.enable": true,
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "posts/*",
        "cmd": "deno fmt && deno task generate"
      },
      {
        "match": "src/*",
        "cmd": "deno fmt && deno lint && deno task prepare"
      }
    ]
  }
}
